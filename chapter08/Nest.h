//============================================================
// File: Nest.h
//============================================================

#ifndef NEST_H
#define NEST_H

/*
    ============================================================
    Nest.h
    ============================================================

    このファイルでは、カッコウ探索
    (Cuckoo Search, CS)
    における 1 個の巣 Nest を定義する。

    ------------------------------------------------------------
    カッコウ探索の基本思想
    ------------------------------------------------------------

    カッコウ探索は、
    「カッコウが他の鳥の巣に卵を産みつける」
    という托卵行動を最適化に見立てた手法である。

    最適化アルゴリズムとして読むと、対応関係はおおむね次のようになる。

        巣 (Nest)
            = 1 個の候補解を保持する入れ物

        卵 (egg)
            = その巣に置かれている現在の候補解ベクトル

        新しい卵 (newEgg)
            = 既存解から生成した新候補

        良い卵
            = 評価値の良い候補解

        巣の放棄
            = 悪い候補解を捨ててランダム再初期化する操作

    したがって Cuckoo Search の本質は、

        1. 良い候補解を少し変形した新候補を作る
        2. それが良ければ採用する
        3. 一部の悪い候補を捨てて新しく生成し直す

    という、単純だが強力な
    exploit と explore の組み合わせにある。

    ------------------------------------------------------------
    このコードでの Nest の意味
    ------------------------------------------------------------

    これまでの Dataset 系クラスの流れと整合的に考えると、
    この Nest が持つ egg は

        標準偏回帰係数ベクトル

    を表していると読むのが自然である。

    つまり egg の各成分は説明変数ごとの係数候補であり、
    Nest 1 個が 1 本の回帰モデル候補に対応する。

    その良し悪しは value で測られ、
    文脈上は

        標準化済みデータに対する二乗誤差和

    のような量になっていると考えられる。

    したがってこのコード全体は、

        回帰係数ベクトル空間における損失最小化

    を Cuckoo Search で解いていると解釈できる。

    ------------------------------------------------------------
    Cuckoo Search の中で Nest が担う役割
    ------------------------------------------------------------

    Nest は単なるデータ構造ではなく、
    個体レベルの探索操作を担うクラスである。

    主に次の 2 つの操作が重要である。

        replace(base)
            良い巣 base をもとに新しい卵を生成し、
            必要なら自分の卵を置き換える

        abandon()
            巣を放棄し、ランダムな新しい卵へ作り直す

    つまり Nest は、

        候補解
        + 候補解の改良
        + 候補解の再初期化

    をまとめて持つ、Cuckoo Search の最小単位である。
*/

#include "Dataset.h"
#include "NestSet.h"

class NestSet;

/*
    ============================================================
    定数定義
    ============================================================

    GEN_MAX:
        世代交代数。

        Cuckoo Search を何回繰り返すかを表す。
        1 回の世代で
            - 新しい卵の生成
            - 巣の置換
            - 一部の巣の放棄
        などが行われると考えられる。

    SET_SIZE:
        巣集合のサイズ。

        何個の候補解を同時に保持するかを表す。
        大きいほど探索空間の多様な領域を同時に見られるが、
        計算コストも増える。

    ABA_RATE:
        放棄する巣の割合。

        Cuckoo Search の重要なパラメータであり、
        悪い巣のうちどれくらいを捨てて新しく作り直すかを決める。

        値が大きいほど探索は多様化しやすいが、
        良い解の蓄積が壊れやすくなることもある。

        値が小さいほど安定的に exploit しやすいが、
        停滞しやすくなる可能性がある。

    ALPHA:
        ステップ長調整パラメータ。

        Levy flight によって生成される移動量の全体スケールを決める。

        大きいと一度の更新で大きく飛びやすくなり、
        探索範囲を広く見やすい。

        小さいと局所的で細かい更新になる。

    BETA:
        Levy flight のスケーリング指数。

        Levy 分布の裾の重さを決める重要なパラメータである。
        Cuckoo Search ではこの Levy flight が
        「ときどき大きく跳ぶ」
        探索性を生み出す。

        BETA が適切に選ばれることで、
        近傍探索と遠距離ジャンプが両立される。

    PI:
        円周率。

        標準正規乱数や Levy flight の係数計算で使う。

    COEF_MIN, COEF_MAX:
        回帰係数の探索範囲。

        この問題では egg が標準偏回帰係数ベクトルを表すと考えられるため、
        各成分はこの範囲内で初期化・再生成される想定である。

    RAND_01:
        0 以上 1 以下の一様乱数。

        各種ランダム生成の基礎になる。

    RAND_N:
        標準正規分布に従う乱数。

        Box-Muller 法に相当する形で定義されている。
        Levy flight のステップ長生成に使うための基礎乱数である。

    NUME, DENOM, SIGMA:
        Levy flight に用いる係数。

        Cuckoo Search の代表的な実装では、
        Mantegna の方法で Levy flight のステップ長を作ることが多い。

        そのとき

            u ~ N(0, sigma^2)
            v ~ N(0, 1)
            step = u / |v|^(1/beta)

        のような形を使う。

        ここで SIGMA はその u の標準偏差に対応している。

        つまりこの定数群は、
        「Levy flight を数値的に生成するための準備」
        を表している。

        これにより Cuckoo Search は、
        普段は細かく探索しつつ、
        ときどき遠くへ大きくジャンプできる。

        この性質が局所最適脱出に効く。
*/
#define GEN_MAX 1000
#define SET_SIZE 100
#define ABA_RATE 0.1
#define ALPHA 0.1
#define BETA 1.5
#define PI 3.141592
#define COEF_MIN -1
#define COEF_MAX 1
#define RAND_01 ((double) rand() / RAND_MAX)
#define RAND_N (sqrt(-2.0 * log(RAND_01)) * cos(2 * PI * RAND_01))
#define NUME (tgamma(1 + BETA) * sin(PI * BETA / 2))
#define DENOM (tgamma((1 + BETA) / 2) * BETA * pow(2.0, (BETA - 1) / 2))
#define SIGMA pow(NUME / DENOM, 1 / BETA)

class Nest {
public:
    /*
        --------------------------------------------------------
        コンストラクタ
        --------------------------------------------------------

        この巣が属する NestSet を受け取り、
        1 個の候補解として初期化する。

        典型的にはここで

            - egg の領域確保
            - newEgg の領域確保
            - egg のランダム初期化
            - 初期評価 value の計算

        を行う。

        つまりコンストラクタ終了時点で、
        Nest はすでに
            「位置ベクトルを持つ 1 個の候補解」
        として完成していることが期待される。
    */
    Nest(NestSet* argNS);

    /*
        --------------------------------------------------------
        デストラクタ
        --------------------------------------------------------

        egg や newEgg のような動的配列を内部に持つ場合は、
        それらの解放責任を持つ。
    */
    ~Nest();

    /*
        --------------------------------------------------------
        replace(base)
        --------------------------------------------------------

        卵を置換する。

        これは Cuckoo Search の exploit 側の中心操作である。

        典型的には、
        良い巣 base をもとに Levy flight などで新しい候補解 newEgg を生成し、
        その評価が現在の egg より良ければ置き換える。

        流れとしては概念的に

            1. base を基準に新しい卵 newEgg を作る
            2. newEgg の評価値を計算する
            3. newEgg が良ければ egg を置換する

        となる。

        ここで重要なのは、単なる局所微小変化ではなく、
        Levy flight により
            「たまに大きく跳ぶ」
        候補が生成されうることである。

        そのため replace() は、
        exploit だけでなく中距離・長距離探索の役割も併せ持つ。

        引数 base:
            置換候補を作るときの基準巣。
            一般には良い巣やランダムに選ばれた巣が渡される。
    */
    void replace(Nest* base);

    /*
        --------------------------------------------------------
        abandon()
        --------------------------------------------------------

        巣を放棄する。

        これは Cuckoo Search の explore 側の中心操作である。

        悪い巣をそのまま残しておくと、
        探索が停滞しやすくなる。
        そこで一部の巣を放棄し、
        ランダムな新しい卵に作り直すことで
        新しい探索領域を開拓する。

        概念的には

            1. egg をランダム再初期化する
            2. value を再計算する

        という操作になる。

        これは
            「局所最適から抜け出すためのリスタート機構」
        と考えると分かりやすい。

        ABA_RATE と組み合わせることで、
        集団の何割をどの程度の頻度でリフレッシュするかが決まる。
    */
    void abandon();

    /*
        ns:
            この巣が属している巣集合。

            Nest 単独では
                - 問題次元数
                - データセット
                - 集合全体の情報
            を知らないため、
            ns を通じて共有情報へアクセスする。

            つまり Nest は NestSet に埋め込まれた探索主体である。
    */
    NestSet* ns;

    /*
        egg:
            現在この巣にある卵、すなわち現在の候補解ベクトル。

            この問題では標準偏回帰係数ベクトルを表すと考えられる。
            したがって egg の各成分は、各説明変数に対応する係数候補である。

            ベクトルの次元数は通常
                ns->dataset->exVarNum
            に一致する。
    */
    double* egg;

    /*
        value:
            現在の卵 egg の評価値。

            これまでの流れから考えると、
            標準化済みデータに対する二乗誤差和のような
                「小さいほど良い」
            量であると解釈するのが自然である。

            したがって Cuckoo Search 全体としては
            この value を最小化する方向へ探索していると読める。
    */
    double value;

private:
    /*
        --------------------------------------------------------
        evaluate(argEgg)
        --------------------------------------------------------

        与えられた卵 argEgg の評価値を計算する。

        これが問題固有の目的関数に対応する。
        つまり Cuckoo Search が何を最適化しているかは、
        最終的にこの関数で決まる。

        現在の egg ではなく引数 argEgg を取る点が重要である。
        これにより

            - 現在の egg
            - 新しく生成した newEgg

        を同じ評価関数で比較できる。

        したがってこの evaluate(argEgg) は、
        候補を採用する前の比較用評価関数として設計されている。

        この問題設定では、
        argEgg を回帰係数ベクトルとみなし、
        標準化済みデータに対する二乗誤差和を返すと考えるのが自然である。
    */
    double evaluate(double* argEgg);

    /*
        newEgg:
            新しい卵、すなわち新候補解を一時的に保持する配列。

            replace(base) の中で、
            現在の egg をいきなり壊すのではなく、
            まず newEgg に新候補を作り、
            それを評価してから
            良ければ入れ替える設計にしていると考えられる。

            これは

                - 比較がしやすい
                - 現在解を安全に保持できる
                - 差し替えを効率よく行える

            という利点がある。

            つまり newEgg は、
            Cuckoo Search における「仮の新しい卵」を表す作業領域である。
    */
    double* newEgg;
};

#endif


/*
    ============================================================
    まとめ
    ============================================================

    この Nest 設計は、カッコウ探索における 1 個の候補解を
    かなり素直に表現している。

    - egg
      現在の候補解

    - value
      その候補解の評価値

    - replace(base)
      良い候補解をもとに新しい候補を生成し、改善を試みる

    - abandon()
      悪い候補解を捨て、新しい候補へ作り直す

    という構造になっており、
    Cuckoo Search の

        exploit
        + explore
        + 淘汰

    が個体レベルでどう起きるかを表現しやすい。

    特に newEgg を別に持っている点は、
    候補比較と安全な置換のために非常に自然な設計である。

    必要なら次に

        - main.cpp
        - NestSet.cpp
        - Nest.cpp

    についても同じ粒度で、
    理論背景つきの詳細コメント版を書ける。
