//============================================================
// File: Swarm.cpp
//============================================================

#include "Swarm.h"

/*
    ============================================================
    Swarm.cpp
    ============================================================

    このファイルでは、粒子群最適化
    (PSO: Particle Swarm Optimization)
    における粒子群全体 Swarm の具体的な振る舞いを実装している。

    ------------------------------------------------------------
    Swarm の理論的な役割
    ------------------------------------------------------------

    PSO では、1 個の候補解を直接少しずつ改良するのではなく、
    複数の粒子を同時に探索空間内で動かし、
    それぞれの粒子が

        - 自分自身がこれまでに見つけた最良位置（pBest）
        - 群れ全体でこれまでに見つかった最良位置（gBest）

    を参考にしながら移動する。

    このとき Swarm は単なる Particle の配列ではなく、

        1. 問題データ Dataset を保持する
        2. 粒子群全体を保持する
        3. 粒子群全体の最良位置 gBest を管理する
        4. 全粒子を 1 時刻ぶん前進させる
        5. 最後に最良解を人間が解釈できる形で出力する

    という、PSO 全体の制御装置である。

    ------------------------------------------------------------
    PSO における Swarm の重要性
    ------------------------------------------------------------

    PSO の特徴は、粒子が単独で探索するのではなく、
    「群れ全体で共有される最良解」を参照して動く点にある。

    つまり、Swarm が持つ

        gBestPos
        gBestValue

    は単なる補助変数ではなく、
    群れ全体の社会的記憶に相当する。

    各粒子はこの gBest に引き寄せられることで、
    良い解が見つかった方向へ集団として収束していく。

    したがって Swarm は、
    PSO の「社会的相互作用」の中心を担っている。

    ------------------------------------------------------------
    この実装が解いている問題
    ------------------------------------------------------------

    これまでの Dataset / Particle 側の流れから考えると、
    各粒子の位置 pos は

        標準偏回帰係数ベクトル

    を表しており、
    value は

        標準化済みデータに対する二乗誤差和

    のような量だと解釈できる。

    そのためこの PSO は、

        回帰係数ベクトルを連続空間内で探索し、
        誤差を最小化する

    問題を解いていると考えられる。

    ------------------------------------------------------------
    この実装が前提としている最適化の向き
    ------------------------------------------------------------

    コード中で

        if (particle[best]->value > particle[i]->value)

    や

        if (gBestValue > particle[i]->value)

    のような比較をしているため、
    value が小さいほど良い解である。

    したがってこの PSO 実装は

        最小化問題

    を前提としている。

    たとえば回帰問題なら、
    value は誤差や損失とみなすのが自然である。
*/


//============================================================
// コンストラクタ
// fileName: データセットのファイル名
//============================================================
Swarm::Swarm(char* fileName)
{
    int i, best;

    /*
        まず問題データ Dataset を生成する。

        Dataset は、

            - 説明変数データ
            - 目的変数データ
            - 標準化データ
            - 回帰式への変換処理

        などを保持している。

        この PSO では、粒子の位置 pos が回帰係数ベクトルに対応すると考えられるため、
        dataset は「探索対象の意味」を与える問題定義クラスになっている。

        つまり Swarm は、
        最初に「何を最適化するのか」という土台を dataset によって準備する。
    */
    dataset = new Dataset(fileName);

    /*
        粒子群を保持する配列を確保する。

        particle は Particle* の配列であり、
        particle[i] が i 番目の粒子を指す。

        SWARM_SIZE 個の粒子を同時に飛ばすことで、
        探索空間の複数領域を並列に探索できる。

        これは PSO が単一点探索ではなく、
        集団ベース最適化であることを表している。
    */
    particle = new Particle*[SWARM_SIZE];

    /*
        best は「初期粒子群の中で最も良い粒子の添字」を表す。

        最初は 0 番粒子を仮の最良としておき、
        残りの粒子を生成しながら比較して更新する。

        ここでの best はコンストラクタ内部だけの一時変数であり、
        最終的には gBestPos / gBestValue の初期値を決めるために使われる。
    */
    best = 0;

    /*
        粒子を 1 個ずつ生成する。

        Particle のコンストラクタの中では通常、

            - 位置 pos の初期化
            - 速度 velocity の初期化
            - 評価値 value の計算
            - パーソナルベスト pBest の初期化

        が行われる。

        つまりこのループが終わる時点で、
        粒子群はすでに「探索を開始できる状態」になっている。
    */
    for (i = 0; i < SWARM_SIZE; i++) {
        particle[i] = new Particle(this);

        /*
            value が小さいほど良い問題なので、
            より小さい value を持つ粒子を見つけたら best を更新する。

            これにより、初期粒子群から群れ全体の最良粒子候補を決める。
        */
        if (particle[best]->value > particle[i]->value) {
            best = i;
        }
    }

    /*
        gBestPos を確保する。

        gBestPos は、粒子群全体でこれまでに見つかった最良位置を保存するベクトルである。
        ベクトルの次元数は説明変数の個数 exVarNum に一致する。

        つまり gBestPos は、
        「群れ全体で共有される最良の回帰係数ベクトル」
        を表す。

        PSO では各粒子がこの gBestPos に引かれて移動するため、
        これはアルゴリズム全体の中心的な共有情報である。
    */
    gBestPos = new double[dataset->exVarNum];

    /*
        初期粒子群の中で最も良かった粒子の位置を gBestPos にコピーする。

        ここでは要素ごとにコピーしている。
        これは

            gBestPos が独立した保存領域

        であることを意味する。

        そのため、後で particle[best] 自身が移動しても、
        gBestPos には「その時点での最良位置」が保持される。

        これは PSO の「履歴としてのグローバルベスト」を正しく実装するうえで重要である。
    */
    for (i = 0; i < dataset->exVarNum; i++) {
        gBestPos[i] = particle[best]->pos[i];
    }

    /*
        最良評価値も保存する。

        gBestValue は、
        「探索開始から現在までの最良スコア」を表す。

        今後 swarm->move() を呼ぶたびに、
        粒子群の中にこれより良い粒子が現れたら更新される。
    */
    gBestValue = particle[best]->value;
}


//============================================================
// デストラクタ
//============================================================
Swarm::~Swarm()
{
    int i;

    /*
        各粒子を解放する。

        particle は Particle* の配列であり、
        各要素が動的確保された Particle オブジェクトを指しているため、
        まずそれぞれを delete する必要がある。

        粒子群サイズ SWARM_SIZE は比較的大きくなりやすいので、
        このようなメモリ管理を正しく行うことは重要である。
    */
    for (i = 0; i < SWARM_SIZE; i++) {
        delete particle[i];
    }

    /*
        Particle* 配列そのものを解放する。
    */
    delete[] particle;

    /*
        グローバルベスト位置ベクトルを解放する。
    */
    delete[] gBestPos;

    /*
        問題データを解放する。

        Swarm は粒子群全体の所有者であり、
        Dataset も Swarm の寿命に従って管理される。
    */
    delete dataset;
}


//============================================================
// 粒子を移動し，グローバルベストを更新する
//============================================================
void Swarm::move()
{
    int i, best;

    /*
        この関数は、PSO における 1 時刻ぶんの群れ全体更新に対応する。

        ここで行っていることは大きく 2 段階に分かれる。

            1. すべての粒子を移動させる
            2. その結果にもとづいて gBest を更新する

        つまり move() は、
        Swarm 全体の時間発展そのものである。

        GA のような「世代交代」というより、
        PSO では「時刻 t から t+1 への状態遷移」と見たほうが自然である。
    */

    //--------------------------------------------------------
    // 1. すべての粒子を移動する
    //--------------------------------------------------------

    /*
        best = -1 は、
        この時刻の移動後に「gBest を更新すべき粒子が見つかったかどうか」
        を表すための番兵値である。

        まだ見つかっていない状態を -1 で表している。
    */
    best = -1;

    /*
        すべての粒子について Particle::move() を呼ぶ。

        Particle::move() の中では通常、

            - 速度 velocity の更新
            - 位置 pos の更新
            - 新位置での評価
            - pBest の更新

        が行われる。

        したがってこのループが終わった時点で、
        粒子群全体は「1 時刻後の状態」へ進んでいる。

        Swarm はここで個々の粒子の細かな運動式には立ち入らず、
        「群れ全体を一斉に進める」ことに専念している。

        これは責務分離として自然である。
    */
    for (i = 0; i < SWARM_SIZE; i++) {
        particle[i]->move();

        /*
            移動後の粒子の value が現在の gBestValue より小さければ、
            その粒子はこれまでの群れ全体の最良解を更新する候補になる。

            ここで注意すべきなのは、
            gBest は「その時刻の粒子群の最良」ではなく、
            「探索開始から現在までの通算最良」である点である。

            したがって比較対象は
                現在の gBestValue
            であり、
            単なる時刻内ランキングではない。
        */
        if (gBestValue > particle[i]->value) {
            best = i;
        }
    }

    //--------------------------------------------------------
    // 2. グローバルベストを更新する
    //--------------------------------------------------------

    /*
        best != -1 なら、
        今回の移動でこれまでの gBest より良い粒子が見つかったことを意味する。

        その場合、その粒子の位置を gBestPos にコピーし、
        評価値を gBestValue に保存する。

        これにより Swarm は、
        群れ全体で共有される最良解記憶を更新する。

        各粒子は次回移動時にこの新しい gBestPos を参照するため、
        良い解が見つかるたびに群れ全体の探索方向が少し変化することになる。

        これが PSO における「社会的共有」の実体である。
    */
    if (best != -1) {

        /*
            最良粒子の位置ベクトルを gBestPos にコピーする。

            ここでも shallow copy ではなく要素ごとのコピーを行っているため、
            粒子が次の時刻に動いても、記録された gBestPos は保持される。

            これは PSO における「履歴としての最良位置」を保つために重要である。
        */
        for (i = 0; i < dataset->exVarNum; i++) {
            gBestPos[i] = particle[best]->pos[i];
        }

        /*
            グローバルベスト評価値も更新する。

            main で毎時刻表示している最良評価値は、この値である。
            したがって gBestValue の推移を見ることで、
            群れ全体がどの程度うまく探索できているかを追跡できる。
        */
        gBestValue = particle[best]->value;
    }
}


//============================================================
// 結果を表示する
//============================================================
void Swarm::printResult()
{
    /*
        gBestPos は標準偏回帰係数ベクトルだと考えられるので、
        まず dataset->setCoef(gBestPos) によって、
        元のスケールの偏回帰係数へ変換する。

        PSO が探索していたのは標準化空間上の係数ベクトルであり、
        そのままでは人間にとって解釈しにくい。

        そこで、最終結果表示時だけ元データスケールへ戻す。

        これは
            探索は数値的に扱いやすい標準化空間で行い、
            結果は解釈しやすい元空間で示す
        という設計である。
    */
    dataset->setCoef(gBestPos);

    /*
        変換後の重回帰式を表示する。

        これにより、PSO が最終的に見つけた最良解が
        実際にはどのような回帰式なのかを確認できる。

        つまり printResult() は、
        最適化の数値結果を問題文脈へ戻して解釈可能にする出口である。
    */
    dataset->printEquation();
}


/*
    ============================================================
    まとめ
    ============================================================

    この Swarm.cpp の本質は、
    粒子群全体を管理しながら、
    群れ全体で共有される最良解 gBest を維持・更新する点にある。

    - コンストラクタ
      問題データと初期粒子群を作り、初期 gBest を決める

    - move()
      全粒子を 1 時刻進め、その結果から gBest を更新する

    - printResult()
      最終的な gBest を回帰式として解釈可能な形で出力する

    つまり Swarm は、

        粒子群の現在状態
        + 群れ全体の共有記憶
        + 時間発展の管理

    を統括する、PSO の中心クラスである。

    個々の粒子は自分の pBest と群れの gBest に引かれて動くが、
    その gBest を管理するのがまさに Swarm である。

    この意味で Swarm は、
    PSO における「集団知のハブ」と言える。
