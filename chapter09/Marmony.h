//============================================================
// File: Harmony.h
//============================================================

#ifndef HARMONY_H
#define HARMONY_H

/*
    ============================================================
    Harmony.h
    ============================================================

    このファイルでは、ハーモニーサーチ
    (Harmony Search, HS)
    における 1 個の「ハーモニー」を表す Harmony クラスを定義する。

    ------------------------------------------------------------
    ハーモニーサーチの基本思想
    ------------------------------------------------------------

    Harmony Search は、音楽家が即興演奏をしながら
    より良い和音の組合せを探していく過程を
    最適化アルゴリズムとして抽象化したものである。

    音楽的な比喩を最適化へ対応づけると、

        - 1 個のハーモニー
            = 1 個の候補解

        - ハーモニーメモリ
            = これまでに得られた候補解の集合

        - 即興で新しい和音を作る
            = 既存解を参考に新しい候補解を生成する

        - 良いハーモニーを記憶に残す
            = 良い候補解を保持し、悪い解を置き換える

    となる。

    したがって Harmony クラスは、
    ハーモニーサーチにおける
        「1 個の候補解」
    に対応する最小単位である。

    ------------------------------------------------------------
    このコードでの Harmony の意味
    ------------------------------------------------------------

    これまでの Dataset クラスの流れと整合的に考えると、
    この Harmony が持つ chord は

        標準偏回帰係数ベクトル

    を表していると読むのが自然である。

    つまり chord の各成分は、
    各説明変数に対応する係数候補であり、

        chord[0], chord[1], ..., chord[p-1]

    が 1 本の回帰モデルを定める。

    この回帰モデルの良し悪しを表すのが value である。

    ------------------------------------------------------------
    value の意味
    ------------------------------------------------------------

    HarmonySearch では、各候補解の良さを評価関数で測る。
    この問題設定では、evaluate() の中でおそらく

        標準化済みデータに対する残差平方和

    のような値を計算していると考えられる。

    数式的には

    $$
    \sum_{i=1}^{n}
    \left(
    y_i - \sum_{j=1}^{p}\beta_j x_{ij}
    \right)^2
    $$

    のような形であり、

        value が小さいほど良い

    最小化問題として解釈するのが自然である。

    したがってこの Harmony クラスは、

        「ある回帰係数ベクトル候補」
        と
        「その候補の評価値」

    をひとまとめにした個体クラスである。

    ------------------------------------------------------------
    Harmony Search における新解生成の考え方
    ------------------------------------------------------------

    Harmony Search の中核は、
    新しい Harmony を作るときに各次元の値を

        1. ハーモニーメモリから選ぶ
        2. 必要なら少し微調整する
        3. ある確率では完全ランダムに生成する

    というルールで決める点にある。

    このクラスの renew() は、
    まさにその「新しいハーモニーへの変更」を担当すると考えられる。

    したがって Harmony は単なる静的な候補解ではなく、

        - 自分自身を新しい候補解へ作り直す
        - その評価値を計算する

    という振る舞いも持つ。

    ------------------------------------------------------------
    このクラスの役割
    ------------------------------------------------------------

    Harmony クラスは主に次の責務を持つ。

        1. chord
           現在の候補解ベクトルを保持する

        2. value
           現在候補解の評価値を保持する

        3. renew()
           ハーモニーメモリを参照して新しい候補解へ変更する

        4. evaluate()
           現在の候補解の評価値を計算する

    つまり Harmony は、

        候補解
        + 候補解生成
        + 候補解評価

    をまとめて持つ、HS の最小単位である。
*/

#include "Dataset.h"
#include "HarmonyMemory.h"

class HarmonyMemory;

/*
    ============================================================
    定数定義
    ============================================================

    REPEAT_NUM:
        繰返し数。

        Harmony Search を何回更新するかを表す。
        1 回の更新ごとに新しい Harmony を 1 個生成し、
        必要ならメモリ中の最悪解と置き換える、という流れになる。

    HM_SIZE:
        ハーモニーメモリのサイズ。

        何個の候補解を同時に記憶するかを表す。
        大きいほど多様な候補解を保持できるが、
        評価・比較コストは増える。

    R_A:
        選択比率。

        一般的な HS の用語でいえば
        HMCR (Harmony Memory Considering Rate)
        に近い役割のパラメータである。

        これは、新しい Harmony を作るときに
        「その次元の値を既存のハーモニーメモリから選ぶ確率」
        を表すと考えられる。

        値が大きいほど、
            過去に得た良い解の成分を再利用しやすくなる。

        これは exploit を強める方向に働く。

    R_P:
        値調整比率。

        一般的な HS の用語でいえば
        PAR (Pitch Adjusting Rate)
        に近い役割である。

        これは、ハーモニーメモリから選んだ値を
        さらに少し微調整する確率を表すと考えられる。

        値が大きいほど、
            既存の良い値の近傍を細かく探索しやすくなる。

        つまり局所探索の強さを決めるパラメータである。

    BANDWIDTH:
        バンド幅。

        値調整を行うときの変化量の大きさを表す。

        HS では典型的に

            x_new = x_old + bandwidth * noise

        のような形で微調整を行う。

        値が大きいほど探索は荒くなり、
        値が小さいほど微調整的な探索になる。

    COEF_MIN, COEF_MAX:
        回帰係数探索範囲。

        この問題では chord が標準偏回帰係数ベクトルを表していると考えられるため、
        各成分はこの範囲内で初期化・ランダム生成される想定である。

    RAND_01:
        0 以上 1 以下の一様乱数。

        Harmony Search における
            - メモリ参照するかどうか
            - 値調整するかどうか
            - ランダム生成するかどうか
        の判断に用いられる基本乱数である。
*/
#define REPEAT_NUM 1000
#define HM_SIZE 100
#define R_A 0.8
#define R_P 0.3
#define BANDWIDTH 0.1
#define COEF_MIN -1
#define COEF_MAX 1
#define RAND_01 ((double) rand() / RAND_MAX)

class Harmony {
public:
    /*
        --------------------------------------------------------
        コンストラクタ
        --------------------------------------------------------

        この Harmony が属する HarmonyMemory を受け取り、
        1 個の候補解として初期化する。

        典型的にはここで

            - chord の領域確保
            - 各成分の初期化
            - 初期評価値 value の計算

        を行う。

        初期化時には通常、
        各成分を探索範囲 [COEF_MIN, COEF_MAX] の一様乱数で
        与えることが多い。

        つまりコンストラクタ終了時点で、
        Harmony はすでに
            「位置ベクトルと評価値を持つ 1 個の候補解」
        として完成しているのが自然である。
    */
    Harmony(HarmonyMemory* argHM);

    /*
        --------------------------------------------------------
        デストラクタ
        --------------------------------------------------------

        chord のような動的配列を内部で持つ場合は、
        その解放責任を持つ。
    */
    ~Harmony();

    /*
        --------------------------------------------------------
        renew()
        --------------------------------------------------------

        新しいハーモニーに変更する。

        これは Harmony Search の本体に対応する重要な関数である。

        典型的には各次元について

            1. 確率 R_A でハーモニーメモリから値を選ぶ
            2. その後、確率 R_P で少し値調整する
            3. そうでなければランダムに新しい値を生成する

        という流れをとる。

        つまり renew() は、

            過去の良い解の記憶を利用しながら
            少しずつ新しい候補解を即興生成する

        処理に相当する。

        音楽の比喩でいえば、
            「過去に良かった和音を参考にしながら、
             少し変えた新しい和音を試す」
        という操作である。
    */
    void renew();

    /*
        hm:
            この Harmony が属している HarmonyMemory。

            Harmony 単独では
                - 問題次元数
                - データセット
                - ハーモニーメモリ中の既存解
            を知らないため、
            hm を通じて共有情報へアクセスする。

            つまり Harmony は HarmonyMemory に埋め込まれた候補解である。
    */
    HarmonyMemory* hm;

    /*
        chord:
            現在の和音列、すなわち現在の候補解ベクトル。

            この問題では標準偏回帰係数ベクトルを表していると考えられる。
            したがって chord の各成分は、各説明変数に対応する係数候補である。

            ベクトル次元数は通常
                hm->dataset->exVarNum
            に一致する。

            音楽的比喩では「和音列」だが、
            数学的には p 次元の実数ベクトル候補である。
    */
    double* chord;

    /*
        value:
            現在の chord に対応する評価値。

            この問題設定では、
            標準化済みデータに対する残差平方和のような
                「小さいほど良い」
            値だと解釈するのが自然である。

            Harmony Search 全体としては、
            この value を最小化する方向へ探索していると読める。
    */
    double value;

private:
    /*
        --------------------------------------------------------
        evaluate()
        --------------------------------------------------------

        現在の chord に対する評価値を計算する。

        これが問題固有の目的関数に対応する。
        つまり HS が最終的に何を最適化しているかは、
        この関数で決まる。

        今回の問題設定では、
        chord を回帰係数ベクトルとみなし、
        標準化済みデータに対する二乗誤差和を計算すると考えるのが自然である。

        したがって evaluate() は、
            「現在の和音列がどれだけ良い解か」
        を数値で表す最重要関数である。
    */
    void evaluate();
};

#endif


/*
    ============================================================
    まとめ
    ============================================================

    この Harmony 設計は、ハーモニーサーチにおける 1 個の候補解を
    かなり素直に表現している。

    - chord
      現在の候補解ベクトル

    - value
      その候補解の評価値

    - renew()
      ハーモニーメモリを利用して新しい候補解へ変更する

    - evaluate()
      現在候補解の良し悪しを計算する

    という構造になっており、
    Harmony Search の

        メモリ参照
        + 値調整
        + ランダム生成

    の考え方を個体レベルで表現しやすい。

    特に R_A, R_P, BANDWIDTH を明示的に持っていることで、

        - メモリをどれくらい活用するか
        - どれくらい局所調整するか
        - どれくらい細かく動かすか

    が明確に分離されているのがよい。

    必要なら次に

        - main.cpp
        - HarmonyMemory.cpp
        - Harmony.cpp

    についても同じ粒度で、
    理論背景つきの詳細コメント版を書ける。
