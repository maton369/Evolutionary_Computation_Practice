//============================================================
// File: main.cpp
//============================================================

#include "Population.h"

/*
    ============================================================
    main.cpp
    ============================================================

    このファイルは、ホタルアルゴリズム
    (Firefly Algorithm, FA)
    全体の外側の実行フローを記述している。

    main 自体は短いが、その短さは設計上望ましい。
    具体的な役割分担は次のようになっている。

        - Dataset
          問題データの読み込み、標準化、回帰式への変換を担当する

        - Firefly
          1 個の候補解を表し、自分より明るいホタルへ向かって移動する

        - Population
          ホタル群全体を保持し、全体の移動と最良解管理を担当する

        - main
          反復の外側ループだけを担当する

    つまり main は、

        「何回ホタル群を動かすか」

    だけを書き、詳細なアルゴリズムは Population / Firefly 側へ
    委譲している。

    これはメタヒューリスティクス実装として見通しの良い構成である。
*/

int main()
{
    int i;
    Population* pop;

    /*
        疑似乱数の seed を現在時刻で初期化する。

        ホタルアルゴリズムは確率的最適化法であり、

            - 初期位置の生成
            - ランダム移動項
            - 個体間相互作用の揺らぎ

        などで乱数を使う。

        そのため seed によって探索軌跡が変化する。

        実験再現性が重要な場合は固定 seed を使い、
        普段の探索では time(NULL) による時刻 seed を使うことが多い。
    */
    srand((unsigned int)time(NULL));

    /*
        Population を生成する。

        ここで通常は

            - Dataset の読み込み
            - 初期ホタル群の生成
            - 初期最良ホタルの記録

        が行われる。

        つまり Population 生成時点で、
        ホタル群全体はすでに探索を開始できる状態になっている。
    */
    pop = new Population((char*)"sampledata.csv");

    /*
        ホタルアルゴリズムの時間発展ループ。

        FA は GA のように「世代交代」するというより、
        個体群が時刻ごとに少しずつ動いていく
        動的系として捉えるほうが自然である。

        したがってここでは i を「世代」ではなく
        「時刻」と呼んでいる。

        各時刻で

            pop->move();

        を 1 回呼ぶことで、
        ホタル群全体の相互作用を 1 ステップ進める。
    */
    for (i = 1; i <= TIME_MAX; i++) {
        pop->move();

        /*
            各時刻で現在の最良ホタルの強度を表示する。

            これにより、

                - 探索がどの程度進んでいるか
                - 最良値が改善しているか
                - 途中で停滞していないか

            を簡易的に観察できる。

            ホタルアルゴリズムでは「明るさ」が良さに対応するため、
            ここでは bestInt を出力している。

            ただし、内部的に最小化問題を扱っている場合は、
            「明るい = 損失が小さい」
            という対応づけになっているはずである。
        */
        printf("時刻%d：光の最高強度%f\n", i, pop->bestInt);
    }

    /*
        最終結果を表示する。

        今回の問題設定では、
        bestPos は標準偏回帰係数ベクトルだと考えられる。
        そのため printResult() の中では通常

            - bestPos を元スケールの偏回帰係数へ変換する
            - 重回帰式を表示する

        といった処理が行われる。

        つまりここは、
        最適化結果を人間が解釈可能な形へ戻す出口である。
    */
    pop->printResult();

    /*
        後始末。

        Population が所有する

            - Firefly 群
            - bestPos
            - Dataset

        なども連鎖的に解放される想定である。
    */
    delete pop;

    return 0;
}


//============================================================
// File: Population.cpp
//============================================================

#include "Population.h"

/*
    ============================================================
    Population.cpp
    ============================================================

    このファイルでは、ホタルアルゴリズム
    (Firefly Algorithm, FA)
    におけるホタル群全体 Population の具体的な振る舞いを実装している。

    ------------------------------------------------------------
    Population の理論的な役割
    ------------------------------------------------------------

    FA では各 Firefly が 1 個の候補解であり、
    各個体は「自分より明るいホタル」に引き寄せられて移動する。

    そのため Population は単なる個体配列ではなく、

        - 問題データ Dataset を保持する
        - ホタル群 firefly を保持する
        - ホタル同士の相互作用を全体として管理する
        - 最も明るいホタル（最良解）を記録する

    という、アルゴリズム全体の制御装置である。

    PSO の Swarm や ABC の FlowerSet と似た役割だが、
    FA では

        「各個体が群れ全体のベストだけを見る」のではなく、
        「各個体が他の個体との相対的な明るさ比較にもとづいて移動する」

    という違いがある。

    ------------------------------------------------------------
    この実装における最良解の意味
    ------------------------------------------------------------

    Population は

        bestPos
        bestInt

    を持っている。

    ここで

        bestPos  : これまでに見つかった最も良い位置
        bestInt  : その位置に対応する最良の光強度

    を表す。

    ホタルアルゴリズムでは通常「明るいほど良い」と説明するが、
    実装によっては内部的には最小化問題を扱い、
    そこから intensity を導いている場合もある。

    したがって bestInt は、
    「最も良いホタルを比較・記録するための代表値」
    と理解するとよい。

    ------------------------------------------------------------
    この Population が行う処理の流れ
    ------------------------------------------------------------

    Population の処理は大きく

        1. 初期ホタル群を生成する
        2. 初期最良ホタルを決める
        3. 各時刻で全ホタルペアの相互作用を実行する
        4. そのたびに最良ホタルを更新する
        5. 最終的な bestPos を回帰式として出力する

    という流れになる。

    特に move() の中で、

        for i
            for j
                firefly[i] を firefly[j] に対して移動させる

    という二重ループになっている点が、
    FA の「個体間相互作用モデル」をよく表している。

    各ホタルは他のホタルを見て、
    より明るい相手にだけ引き寄せられる。
    そのためアルゴリズムの本質は、
    個体間ペアの相互作用を全組合せ的に評価するところにある。
*/


//============================================================
// コンストラクタ
// fileName: データセットのファイル名
//============================================================
Population::Population(char* fileName)
{
    int i, best;

    /*
        まず問題データ Dataset を生成する。

        Dataset は、

            - 説明変数データ
            - 目的変数データ
            - 標準化データ
            - 回帰係数変換処理

        を保持している。

        この問題設定では、各ホタルの位置 pos は
        標準偏回帰係数ベクトルを表していると考えられるので、
        dataset は「何を最適化しているのか」を与える問題定義クラスである。
    */
    dataset = new Dataset(fileName);

    /*
        ホタル群を保持する配列を確保する。

        firefly は Firefly* の配列であり、
        firefly[i] が i 番目のホタルを指す。

        POP_SIZE 個の候補解を同時に持つことで、
        探索空間の複数領域を並列に探索できる。

        これはメタヒューリスティクスにおける集団探索の基本的な利点である。
    */
    firefly = new Firefly*[POP_SIZE];

    /*
        best は「初期ホタル群の中で最も良いホタルの添字」を表す。

        最初は 0 番ホタルを仮の最良としておき、
        残りを生成しながら比較して更新する。
    */
    best = 0;

    /*
        初期ホタル群を 1 個ずつ生成する。

        各 Firefly のコンストラクタの中では通常、

            - 位置 pos のランダム初期化
            - 光強度 intensity の計算

        が行われる。

        したがって、このループ終了時点で
        Population はすでに探索を開始できる状態になっている。
    */
    for (i = 0; i < POP_SIZE; i++) {
        firefly[i] = new Firefly(this);

        /*
            ここでは「より良いホタル」を探して best を更新している。

            元のコード断片では

                if (firefly[best]->intensity > firefly[i]->intensity)

            のような比較が省略・崩れた形で混じっていたと考えられるが、
            文脈上は「最良ホタルを見つける」という意図である。

            どちら向きの比較になるかは intensity の定義に依存する。
            ここではアルゴリズム上、
                best = 最良ホタル
            を選んでいる、という意味が重要である。
        */
        if (firefly[best]->intensity > firefly[i]->intensity) {
            best = i;
        }
    }

    /*
        bestPos を確保する。

        bestPos は、探索全体を通じて最も良いホタルの位置を保存する。
        次元数は説明変数の数 exVarNum に一致する。

        つまり bestPos は、
        「群れ全体で見つかった最良の標準偏回帰係数ベクトル」
        を表す。
    */
    bestPos = new double[dataset->exVarNum];

    /*
        初期最良ホタルの位置を bestPos にコピーする。

        ここでは shallow copy ではなく要素ごとのコピーを行っている。
        これにより、後で firefly[best] 自身が移動しても、
        bestPos にはその時点での最良位置が保存されたままになる。

        これは「履歴としての最良解」を保持するうえで重要である。
    */
    for (i = 0; i < dataset->exVarNum; i++) {
        bestPos[i] = firefly[best]->pos[i];
    }

    /*
        初期最良強度を保存する。

        bestInt は、
        探索開始時点での群れ全体のベスト指標であり、
        move() のたびに必要なら更新される。
    */
    bestInt = firefly[best]->intensity;
}


//============================================================
// デストラクタ
//============================================================
Population::~Population()
{
    int i;

    /*
        各ホタルを解放する。

        firefly は Firefly* の配列であり、
        各要素が動的確保された Firefly オブジェクトを指している。
        したがってまず各オブジェクトを delete する必要がある。
    */
    for (i = 0; i < POP_SIZE; i++) {
        delete firefly[i];
    }

    /*
        Firefly* 配列そのものを解放する。
    */
    delete[] firefly;

    /*
        最良位置ベクトルを解放する。
    */
    delete[] bestPos;

    /*
        問題データを解放する。

        Population は群れ全体の所有者であり、
        Dataset も Population の寿命に従って管理される。
    */
    delete dataset;
}


//============================================================
// ホタルを移動する
//============================================================
void Population::move()
{
    int i, j, k;

    /*
        この関数は、ホタルアルゴリズムにおける
        「群れ全体の 1 時刻ぶんの相互作用」を実行する。

        FA の核心は、
        各ホタルが他のホタルと比較され、
        より明るい相手に対して移動することにある。

        そのため典型的には二重ループで

            各 i に対して
                各 j を比較対象にする

        という構造になる。

        これは PSO のように gBest 1 点だけを見るのではなく、
        個体間相互作用を通じて群れ全体が再配置されることを意味する。
    */

    /*
        --------------------------------------------------------
        すべてのホタルのペアについて繰り返す
        --------------------------------------------------------

        外側ループ i:
            「今動かす対象のホタル」

        内側ループ j:
            「比較対象となるホタル」

        である。

        つまり firefly[i] は、
        firefly[j] を見て必要なら移動する。

        FA の標準的な考え方では、

            if firefly[j] のほうが明るい
                firefly[i] を firefly[j] に近づける

        という条件分岐が Firefly::move(base) の中に入っていることが多い。

        そのため Population 側は、
        「すべての組み合わせを回す」
        ことだけを担当している。
    */
    for (i = 0; i < POP_SIZE; i++) {
        for (j = 0; j < POP_SIZE; j++) {

            /*
                firefly[i] を firefly[j] に対して移動させる。

                ここでの move(base) の中では通常、

                    - 自分より base のほうが明るいか判定する
                    - 距離を計算する
                    - 距離に応じた誘引度を求める
                    - base 方向への移動項を作る
                    - ランダム揺らぎを加える
                    - intensity を再計算する

                といった処理が行われる。

                つまり Population::move() は
                個体間相互作用の外枠だけを与え、
                実際の移動式は Firefly::move() に委譲している。

                この責務分離は自然である。
            */
            firefly[i]->move(firefly[j]);

            /*
                ------------------------------------------------
                最も明るいホタルの位置を更新する
                ------------------------------------------------

                move() の結果、firefly[i] がこれまでの最良より良くなったなら、
                bestPos と bestInt を更新する。

                ここで重要なのは、
                bestPos は「今の時刻の最良」ではなく、
                「探索開始から現在までの履歴最良」
                として保持されている点である。

                そのため、より良いホタルが現れた瞬間に
                その位置をコピーして保存しておく必要がある。

                元の断片コードでは

                    if (bestInt > firefly[i]->intensity)

                となっているが、
                これは intensity の定義次第では
                比較方向が逆になる可能性がある。

                ただし文脈上の意図は一貫しており、

                    「現時点までで最良のホタルを更新する」

                という処理である。
            */
            if (bestInt > firefly[i]->intensity) {
                for (k = 0; k < dataset->exVarNum; k++) {
                    bestPos[k] = firefly[i]->pos[k];
                }
                bestInt = firefly[i]->intensity;
            }
        }
    }
}


//============================================================
// 結果を表示する
//============================================================
void Population::printResult()
{
    /*
        bestPos は標準偏回帰係数ベクトルだと考えられるので、
        まず dataset->setCoef(bestPos) によって、
        元のスケールの偏回帰係数へ変換する。

        これは

            探索は標準化空間で行い、
            結果表示だけ元スケールへ戻す

        という設計に対応している。

        メタヒューリスティクスでは、
        数値的に扱いやすい空間で探索し、
        最後に人間が解釈しやすい形へ戻すのは自然な流れである。
    */
    dataset->setCoef(bestPos);

    /*
        変換後の重回帰式を表示する。

        これにより、ホタルアルゴリズムが最終的に見つけた最良解が
        実際にどのような回帰式なのかを確認できる。

        つまり printResult() は、
        最適化結果を問題文脈へ戻して解釈可能にする出口である。
    */
    dataset->printEquation();
}


/*
    ============================================================
    まとめ
    ============================================================

    この Population.cpp の本質は、
    ホタル群全体を管理しながら、
    個体間相互作用に基づいて群れ全体を再配置し、
    その中で最良個体を記録し続ける点にある。

    - コンストラクタ
      問題データと初期ホタル群を生成し、初期ベストを決める

    - move()
      すべてのホタルペアを比較し、必要な移動を行い、
      その都度最良ホタルを更新する

    - printResult()
      最終的な最良位置を回帰式として解釈可能な形で出力する

    つまり Population は、

        ホタル群の現在状態
        + 個体間相互作用の管理
        + 最良解の履歴保持

    を統括する、ホタルアルゴリズムの中心クラスである。

    PSO が gBest を中心に動くのに対して、
    FA は個体同士の相対的な明るさを使って動く。
    その違いがこの move() の二重ループ構造によく表れている。
*/ 
