//============================================================
// File: Colony.cpp
//============================================================

#include "Colony.h"

/*
    ============================================================
    Colony.cpp
    ============================================================

    このファイルでは、アリコロニー最適化（ACO: Ant Colony Optimization）における
    Colony クラスの主要処理を実装している。

    Colony は、単なる「アリの集合」ではない。
    ACO 全体の流れ

        1. 問題空間を準備する
        2. 複数のアリに経路を構築させる
        3. その結果をもとにフェロモンを更新する
        4. 反復により良い経路を強化していく

    を統括する中心クラスである。

    ------------------------------------------------------------
    Colony の理論的な役割
    ------------------------------------------------------------

    ACO では、各アリは局所情報しか使わずに経路を選ぶ。
    しかし、その行動結果はフェロモンとして「場」に残り、
    次のアリたちの行動へ影響する。

    つまり Colony は、

    - 個々のアリの局所行動をまとめる
    - それを共有記憶（フェロモン）へ反映する
    - 反復を通して探索全体を進める

    という「集団知の制御装置」である。

    GA でいう Population に近い位置づけだが、
    ACO では「個体そのものを世代交代させる」のではなく、
    「場の情報を更新しながら新しい経路を毎回再構築する」
    という違いがある。

    ------------------------------------------------------------
    この実装の全体フロー
    ------------------------------------------------------------

    Colony の1反復は概念的に次のような流れを取る。

        Colony::selectRoute()
            ↓
        全アリが現在の pheromone と distance を参照して route を構築
            ↓
        Colony::renewPheromone()
            ↓
        pheromone を蒸発させる
            ↓
        各アリの route に基づいて pheromone を加算する

    これを繰り返すことで、

    - 短くて有望な経路は何度も通られやすくなり
    - その結果フェロモンが濃くなり
    - 次の反復でも選ばれやすくなる

    という正のフィードバックが働く。

    ただし蒸発も入っているため、
    昔の情報が永遠に残るわけではなく、
    探索が固定化しすぎないようになっている。
*/


//============================================================
// コンストラクタ
// fileName: データセットのファイル名
//============================================================
Colony::Colony(char* fileName)
{
    int i;

    /*
        Field を生成する。

        Field は探索の舞台であり、
        そこには

            - ノード数 nodeNum
            - ノード間距離 distance
            - エッジ上のフェロモン量 pheromone

        が保持される。

        つまり Colony は、まず問題そのものを表す Field を用意し、
        その上でアリたちを動かす。

        ACO において Field は
        「問題構造 + 共有記憶」
        を表す非常に重要なオブジェクトである。
    */
    field = new Field(fileName);

    /*
        アリ集団を格納する配列を確保する。

        ant は Ant* の配列であり、
        ant[i] が i 番目のアリを指す。

        ANT_NUM 匹のアリを持つことで、
        1回の反復で複数の経路候補を同時に生成できる。

        これは探索空間の複数箇所を並列に見ることに相当し、
        ACO が単一の貪欲法より広い探索を行える理由の1つである。
    */
    ant = new Ant*[ANT_NUM];

    /*
        各アリを生成する。

        Ant のコンストラクタには this を渡している。
        つまり各アリは「自分がどの Colony に属しているか」を知っている。

        これにより Ant は、

            colony -> field -> distance / pheromone

        という形で共有情報へアクセスできる。

        理論的には、アリは独立した解ではなく、
        場に埋め込まれたエージェントである。
        そのため Colony への参照を持つ設計は自然である。
    */
    for (i = 0; i < ANT_NUM; i++) {
        ant[i] = new Ant(this);
    }

    /*
        nume を確保する。

        nume は「経路選択確率の分子」を一時的に格納する補助行列である。

        ACO では、ノード i から j へ進む魅力度を一般に

            (pheromone)^alpha * (heuristic)^beta

        の形で計算する。

        ここで heuristic は通常

            1 / distance

        であり、「近いノードほど魅力的」という局所知識を表す。

        nume[i][j] は、そのような正規化前の魅力度を保存する領域である。

        つまり nume は、
        route 選択時に各候補エッジの「どれくらい選びたくなるか」
        を表す作業用メモリである。

        これは pheromone のような永続的な記憶ではなく、
        各反復・各選択で使う一時的な計算補助である点に注意する。
    */
    nume = new double*[field->nodeNum];
    for (i = 0; i < field->nodeNum; i++) {
        nume[i] = new double[field->nodeNum];
    }
}


//============================================================
// デストラクタ
//============================================================
Colony::~Colony()
{
    int i;

    /*
        まず各 Ant オブジェクトを解放する。

        Colony は ant 配列だけでなく、
        その中の各 Ant インスタンスも所有しているため、
        それぞれ delete する必要がある。

        ACO ではアリ数 ANT_NUM が大きくなりやすいため、
        このような動的メモリを正しく解放することは重要である。
    */
    for (i = 0; i < ANT_NUM; i++) {
        delete ant[i];
    }

    /*
        Ant* 配列そのものを解放する。
    */
    delete[] ant;

    /*
        nume の各行を解放する。

        nume は 2 次元配列として
            行ごとに new
        しているので、
        解放時も各行を delete[] してから
        外側配列を delete[] する必要がある。
    */
    for (i = 0; i < field->nodeNum; i++) {
        delete[] nume[i];
    }

    /*
        外側配列を解放する。
    */
    delete[] nume;

    /*
        最後に Field を解放する。

        Field は問題空間そのものとフェロモン行列を保持するため、
        Colony の寿命と一致して存在するのが自然である。
    */
    delete field;
}


//============================================================
// 経路を選択する
//============================================================
void Colony::selectRoute()
{
    int i, j;

    /*
        この関数は、ACO の「解構築フェーズ」に対応する。

        ここで Colony は2つの仕事をしている。

        1. 各ノード対 (i, j) に対して選択魅力度 nume[i][j] を計算する
        2. 各アリに対して実際の経路構築を行わせる

        つまり、

            共有された魅力度情報を準備する
                ↓
            個々のアリがそれを使って route を作る

        という流れになっている。
    */

    //--------------------------------------------------------
    // 1. 確率の分子を算出する
    //--------------------------------------------------------
    /*
        nume[i][j] は、ノード i から j へ進むときの
        選択確率の「分子」に相当する値である。

        ACO の典型的な遷移確率は、

            p(i,j) ∝ (tau(i,j))^alpha * (eta(i,j))^beta

        で与えられる。

        ここで

            tau(i,j) = フェロモン量
            eta(i,j) = ヒューリスティック情報（通常 1 / distance）
            alpha    = フェロモンの重み
            beta     = ヒューリスティックの重み

        である。

        このコードでは

            PHERO_L = alpha
            HEU_L   = beta

        に対応していると解釈できる。

        よって nume[i][j] は
        「エッジ (i,j) の魅力度」
        を表している。

        値が大きいほど、そのエッジは選ばれやすくなる。
    */
    for (i = 0; i < field->nodeNum; i++) {

        /*
            j < i の領域について計算する。

            pheromone 行列や distance 行列が対称である場合、
            実質的には無向グラフ上のエッジを扱っていると考えられる。

            このコードでは、インデックス順に応じて

                pheromone[j][i]

            を参照している。

            これは三角行列的な扱いを意識している可能性がある。
        */
        for (j = 1; j < i; j++) {
            nume[i][j] =
                pow(field->pheromone[j][i], PHERO_L) *
                pow(1 / field->distance[i][j], HEU_L);
        }

        /*
            j > i の領域について計算する。

            こちらでは

                pheromone[i][j]

            を参照している。

            ヒューリスティック情報として

                1 / distance[i][j]

            を使うことで、距離が短いエッジほど魅力度が高くなる。

            したがってこの実装は、

                フェロモンで「過去の経験」を反映しつつ
                距離で「目先の合理性」も反映する

            という ACO の基本思想を表している。

            なお、元の断片には

                PHERO_L のところが P になっている

            ような崩れが見えるが、
            ここでは文脈上 PHERO_L を使うのが自然である。
        */
        for (j = i + 1; j < field->nodeNum; j++) {
            nume[i][j] =
                pow(field->pheromone[i][j], PHERO_L) *
                pow(1 / field->distance[i][j], HEU_L);
        }
    }

    //--------------------------------------------------------
    // 2. 各アリに経路を選択させる
    //--------------------------------------------------------
    /*
        各アリが、現在の pheromone と nume をもとに
        1 本の route を構築する。

        ここでのポイントは、
        Colony 自身が route の詳細を決めるのではなく、
        実際の経路構築は Ant::selectRoute() に任せている点である。

        つまり Colony は
        「全体に共通の魅力度場を整える」
        役であり、
        Ant は
        「その場の上で1本の解を作る」
        役である。

        この責務分離により、

            集団の制御
            と
            個体の局所意思決定

        が混ざらず、ACO の構造が読みやすくなっている。
    */
    for (i = 0; i < ANT_NUM; i++) {
        ant[i]->selectRoute();
    }
}


//============================================================
// フェロモン量を更新する
//============================================================
void Colony::renewPheromone()
{
    int i, j;

    /*
        この関数は、ACO の「共有記憶更新フェーズ」に対応する。

        route 構築が終わったあと、
        その結果を pheromone に反映する。

        ACO の更新は大きく

            1. 蒸発
            2. 追加

        の2段階で行うのが基本である。

        蒸発だけだと情報が消えていくだけであり、
        追加だけだと古い情報が無限に蓄積してしまう。

        この2つを組み合わせることで、

            - 有望な経路を強調しつつ
            - 過去の誤った偏りを徐々に忘れる

        という探索ダイナミクスが実現される。
    */

    //--------------------------------------------------------
    // 1. フェロモンを蒸発させる
    //--------------------------------------------------------
    /*
        各エッジの pheromone を

            pheromone *= (1 - EVA_R)

        により減衰させる。

        EVA_R は蒸発率であり、
        0 に近いと蒸発が弱く、
        1 に近いと蒸発が強い。

        蒸発の理論的な役割は次の通りである。

        1. 古い情報を忘れる
           以前たまたま通られただけの経路が永遠に有利にならないようにする。

        2. 探索の柔軟性を保つ
           いったん偏った後でも、別の経路へ移る余地を残す。

        3. 正のフィードバックの暴走を防ぐ
           良い経路が見つかったとしても、完全固定ではなく再評価の余地を持つ。

        このコードでは j = i + 1 から回しているため、
        上三角側のみ更新している。
        これは無向グラフの対称性を意識している可能性が高い。
    */
    for (i = 0; i < field->nodeNum; i++) {
        for (j = i + 1; j < field->nodeNum; j++) {
            field->pheromone[i][j] *= 1 - EVA_R;
        }
    }

    //--------------------------------------------------------
    // 2. 各アリによるフェロモン追加分を加算する
    //--------------------------------------------------------
    /*
        各アリが自分の通った route に応じて pheromone を追加する。

        一般には、

            追加量 ∝ PHERO_Q / totalDis

        のように、
        総移動距離 totalDis が短いアリほど
        強いフェロモンを残す設計がよく使われる。

        これにより、短い経路ほど次回以降選ばれやすくなり、
        集団全体として良い経路へ収束していく。

        ここでも Colony 自身が route を直接見て更新するのではなく、
        個々のアリに

            putPheromone()

        を呼ばせている。

        これは

            Colony = 全体制御
            Ant    = 自分の経路結果の反映

        という責務分離になっている。
    */
    for (i = 0; i < ANT_NUM; i++) {
        ant[i]->putPheromone();
    }
}


//============================================================
// フェロモン量を表示する
//============================================================
void Colony::printPheromone()
{
    int i, j;

    /*
        現在の pheromone 行列をそのまま表示する。

        これは単なるデバッグ出力以上の意味を持つ。

        ACO では最終的な最良 route だけでなく、
        pheromone 分布そのものが

            「どのエッジが集団的に有望だと判断されたか」

        を表す内部状態になっている。

        そのため pheromone を表示することで、

            - 学習が特定経路へ集中しているか
            - まだ広く分散しているか
            - 対称性が崩れているか
            - 更新がうまく働いているか

        などを観察できる。

        研究・実験用途では、このような内部状態の可視化は重要である。
    */
    for (i = 0; i < field->nodeNum; i++) {
        for (j = 0; j < field->nodeNum; j++) {
            printf("%8.3f ", field->pheromone[i][j]);
        }
        printf("\n");
    }
}


/*
    ============================================================
    まとめ
    ============================================================

    この Colony.cpp の本質は、
    ACO の集団的な学習サイクルを実装している点にある。

    - コンストラクタ
      問題空間 Field とアリ集団 Ant を準備する

    - selectRoute()
      現在の pheromone と heuristic に基づいて
      各アリが route を構築する

    - renewPheromone()
      その route の良し悪しを共有記憶 pheromone へ反映する

    - printPheromone()
      集団学習の結果としての pheromone 分布を観察する

    つまり Colony は、
    ACO における

        「局所行動の集団的強化」

    を実現する中心クラスである。

    アリ1匹の行動は局所的で単純でも、
    Colony によってそれらが束ねられることで、
    全体として有望な経路を学習していく。

    この点が ACO の核心であり、
    このファイルはそのダイナミクスをコードとして表現している。
*/
