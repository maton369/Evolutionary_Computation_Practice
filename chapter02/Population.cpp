//============================================================
// File: Population.cpp
//============================================================

#include "Population.h"

/*
    ============================================================
    Population.cpp
    ============================================================

    このファイルでは、個体群 Population に関する主要な処理を実装している。
    進化計算において Population は単なる「個体の配列」ではなく、
    探索全体の状態を表す中心的なデータ構造である。

    ------------------------------------------------------------
    Population が担う理論的役割
    ------------------------------------------------------------

    1. 個体群全体を保持する
       進化計算では、1個の解だけではなく、複数の候補解を同時に持つ。
       これにより、探索空間の異なる領域を並列に探索できる。

    2. 個体群全体を評価する
       各個体の適応度を計算し、それらを比較可能な状態にする。
       進化計算では「良い個体を残し、悪い個体を淘汰する」ため、
       個体群全体の評価が必須である。

    3. 個体群に選択圧をかける
       親選択によって、より良い個体ほど次世代へ影響を与えやすくする。
       これが進化の駆動力になる。

    4. 次世代を生成する
       現世代 ind を参照して次世代 nextInd を作る。
       この「現在の集団」と「次の集団」を分ける設計が、
       世代モデルの遺伝的アルゴリズムをきれいに表現している。

    5. 世代交代を管理する
       次世代が完成したら ind と nextInd を入れ替えることで、
       時刻 t の集団を時刻 t+1 の集団へ更新する。

    ------------------------------------------------------------
    このコードが前提としている最適化の向き
    ------------------------------------------------------------

    sort() の実装を見ると、fitness が小さい個体ほど前方へ来るように
    並び替えている。

        ind[0] が最も fitness の小さい個体
        ind[1] がその次
        ...

    したがって、この Population 実装は
    「fitness が小さいほど良い最小化問題」
    を前提に読むのが自然である。

    もし fitness が大きいほど良い最大化問題として使うなら、
    sort() の比較方向や結果表示の解釈を変更する必要がある。
*/


//============================================================
// コンストラクタ
//============================================================
Population::Population()
{
    int i;

    /*
        ind:
            現世代の個体群を指す配列である。
            ind[i] は i 番目の個体を表す。

        nextInd:
            次世代の個体群を指す配列である。

        ここで配列そのものだけでなく、
        各要素の Individual オブジェクトも動的に生成している。

        この設計により、
        - 現世代 ind を読みながら
        - 次世代 nextInd に子個体を書き込む
        という二重バッファ方式の世代交代が可能になる。

        進化計算では、現世代を上書きしながら子個体を作ると、
        まだ親として使うべき個体を途中で壊してしまう危険がある。
        そのため、現世代と次世代を分ける設計は非常に重要である。
    */
    ind = new Individual*[POP_SIZE];
    nextInd = new Individual*[POP_SIZE];

    /*
        個体群サイズ POP_SIZE だけ個体を生成する。

        ここで生成される Individual は通常、
        コンストラクタ内でランダム初期化された染色体を持つと考えられる。

        初期個体群は進化計算の探索開始点であり、
        その多様性が初期探索の広がりを決める。
        個体群が多様であるほど、探索空間の広い領域を同時に探索しやすい。
    */
    for (i = 0; i < POP_SIZE; i++) {
        ind[i] = new Individual();
        nextInd[i] = new Individual();
    }

    /*
        初期個体群を評価する。

        evaluate() は
        - 各個体の適応度を計算し
        - 個体群全体を適応度順に並べ替える
        処理をまとめて行う。

        これにより、Population オブジェクト生成直後から
        個体群が「比較可能な秩序を持った状態」になる。
    */
    evaluate();
}


//============================================================
// デストラクタ
//============================================================
Population::~Population()
{
    int i;

    /*
        Population は多数の Individual を所有しているため、
        デストラクタではそれらをすべて解放する必要がある。

        ここでは
        1. 各 Individual オブジェクトを delete
        2. それらを指している配列自体を delete[]
        の順で解放している。

        進化計算では個体群サイズが大きくなることが多く、
        世代数も多いため、
        メモリ管理を誤ると大きな無駄やリークにつながる。
        したがって Population は探索管理だけでなく、
        リソース管理の責務も持っている。
    */
    for (i = 0; i < POP_SIZE; i++) {
        delete ind[i];
        delete nextInd[i];
    }

    delete[] ind;
    delete[] nextInd;
}


//============================================================
// すべての個体を評価し，適応度順に並び替える
//============================================================
void Population::evaluate()
{
    int i;

    /*
        この関数は Population における「集団評価」の中心である。

        Individual::evaluate() は個体単体の適応度を求める処理だが、
        Population::evaluate() はそれを集団全体へ適用する。

        進化計算では、単に個体を生成するだけでは意味がない。
        生成した個体群を評価し、
        「どの個体が相対的に良いか」を比較可能にして初めて、
        選択・淘汰・エリート保存が実行できる。
    */
    for (i = 0; i < POP_SIZE; i++) {
        ind[i]->evaluate();
    }

    /*
        評価後に個体群をソートする。

        これにより、
        - 上位個体への即時アクセス
        - エリート保存の簡略化
        - 親選択時の順位情報利用
        が可能になる。

        この実装では sort() により fitness の小さい順に並ぶため、
        ind[0] が最良個体となる。
    */
    sort(0, POP_SIZE - 1);
}


//============================================================
// ind[lb]～ind[ub] をクイックソートで並び替える
// lb: 並び替え対象の添え字の下限
// ub: 並び替え対象の添え字の上限
//============================================================
void Population::sort(int lb, int ub)
{
    int i, j, k;
    double pivot;
    Individual* tmp;

    /*
        この sort() は個体群を適応度順に並べ替える関数である。

        ここで並び替えの対象は「個体本体」ではなく、
        個体へのポインタ ind[i] である。
        したがって、swap 時にはポインタを入れ替えるだけで済み、
        個体の中身全体をコピーするより効率が良い。

        また、Population にとってソートは単なる見た目の整理ではない。
        ソートによって
        - 先頭側に良い個体が集まり
        - 後ろ側に悪い個体が集まる
        という秩序が作られる。

        この秩序があることで、
        エリート保存や順位ベース選択が簡単に実装できる。
    */
    if (lb < ub) {
        /*
            中央要素の fitness を pivot とする。

            クイックソートでは pivot を基準に
            - pivot より小さい側
            - pivot より大きい側
            に分割していく。

            このコードでは
                while (ind[i]->fitness < pivot)
                while (ind[j]->fitness > pivot)
            となっているため、
            小さい fitness が前へ来る昇順ソートである。

            よって、この実装は
            「fitness を最小化したい問題」
            に自然に対応している。
        */
        k = (lb + ub) / 2;
        pivot = ind[k]->fitness;

        i = lb;
        j = ub;

        do {
            /*
                左側から pivot 以上の要素が見つかるまで進める。

                つまり、すでに pivot より小さい要素は
                「左側にいて正しい」とみなして飛ばしている。
            */
            while (ind[i]->fitness < pivot) {
                i++;
            }

            /*
                右側から pivot 以下の要素が見つかるまで戻す。

                つまり、すでに pivot より大きい要素は
                「右側にいて正しい」とみなして飛ばしている。
            */
            while (ind[j]->fitness > pivot) {
                j--;
            }

            /*
                左右で見つかった「位置が逆転している要素」を交換する。

                これにより、
                小さい fitness の個体が前へ、
                大きい fitness の個体が後ろへ寄っていく。
            */
            if (i <= j) {
                tmp = ind[i];
                ind[i] = ind[j];
                ind[j] = tmp;
                i++;
                j--;
            }

        } while (i <= j);

        /*
            分割後、左半分 [lb, j] と右半分 [i, ub] に対して
            再帰的に同じ操作を行う。

            これにより、区間全体が昇順に整列される。
        */
        sort(lb, j);
        sort(i, ub);
    }
}


//============================================================
// 世代交代をする
//============================================================
void Population::alternate()
{
    int i, j, p1, p2;
    Individual** tmp;

    /*
        alternate() は Population クラスの中心関数であり、
        1世代分の進化をまとめて実行する。

        この関数全体は、進化計算の流れ

            評価済みの現世代
                ↓
            良い個体を残す
                ↓
            親を選ぶ
                ↓
            交叉で子を作る
                ↓
            突然変異を加える
                ↓
            次世代へ置き換える
                ↓
            新しい世代を評価する

        という状態遷移そのものを表している。

        つまり alternate() は、
        Population を時刻 t から t+1 に進める更新則である。
    */


    //--------------------------------------------------------
    // 1. エリート保存
    //--------------------------------------------------------
    /*
        エリート保存戦略では、現世代で特に優れた個体を
        そのまま次世代へコピーする。

        これにより、
        - せっかく見つけた良い解が交叉や突然変異で壊れるのを防ぐ
        - 世代交代によって性能が急激に悪化するのを防ぐ
        という効果がある。

        ソート済みなので、最良個体は先頭側に並んでいる。
        ここでは ELITE 個の個体を nextInd の先頭へ複製している。

        染色体配列 chrom を要素ごとにコピーしているのは、
        個体の「解の内容」を保存するためである。
    */
    for (i = 0; i < ELITE; i++) {
        for (j = 0; j < N; j++) {
            nextInd[i]->chrom[j] = ind[i]->chrom[j];
        }

        /*
            注意:
            この時点では fitness はコピーしていない。
            ただし、後で evaluate() を呼ぶため、
            次世代に切り替わった後に適応度は再計算される。
        */
    }


    //--------------------------------------------------------
    // 2. 親を選択し，交叉によって残りの子個体を生成する
    //--------------------------------------------------------
    /*
        エリート以外の個体は、親選択と交叉によって生成する。

        ここで Population は
        - 個体群の中から select() で親を選び
        - Individual::crossover() によって子を構成する
        という形で、
        個体群レベルの操作と個体レベルの操作を接続している。

        理論的には、
        親選択は「良い個体により大きな影響力を与える」操作であり、
        交叉は「複数の親が持つ良い部分構造を再結合する」操作である。
    */
    for (; i < POP_SIZE; i++) {
        p1 = select();
        p2 = select();

        /*
            nextInd[i] を子個体として使い、
            ind[p1], ind[p2] を親として交叉する。

            ここでの設計は、
            「子を新たに new する」のではなく、
            既存の nextInd[i] オブジェクトを上書き利用する形である。
            これは世代ごとのメモリ再利用という意味で効率的である。
        */
        nextInd[i]->crossover(ind[p1], ind[p2]);
    }


    //--------------------------------------------------------
    // 3. 突然変異を起こす
    //--------------------------------------------------------
    /*
        突然変異は、通常エリート個体には適用しない。
        その理由は、エリート保存の目的が
        「最良個体を壊さず残すこと」にあるためである。

        したがってこの実装では、i = 1 から突然変異をかけている。
        ただし、より一般には
            i = ELITE から
        と書いたほうが、ELITE の値が変わっても整合する設計になる。

        突然変異の理論的役割は、
        - 個体群の多様性維持
        - 局所解からの脱出
        - 交叉だけでは生まれない新しい遺伝子配置の導入
        である。
    */
    for (i = 1; i < POP_SIZE; i++) {
        nextInd[i]->mutate();
    }


    //--------------------------------------------------------
    // 4. 次世代を現世代へ切り替える
    //--------------------------------------------------------
    /*
        次世代 nextInd の生成が完了したら、
        ind と nextInd のポインタを入れ替える。

        これにより、
        - これまで nextInd に入っていた子個体群が
          新しい現世代 ind になる
        - 旧 ind は次回の作業領域として nextInd になる

        という役割交換が行われる。

        このポインタ交換は、
        個体群全体を1つずつコピーし直すよりもはるかに効率的である。

        進化計算における「世代交代」が、
        実装上はこの swap 操作に対応している点は重要である。
    */
    tmp = ind;
    ind = nextInd;
    nextInd = tmp;


    //--------------------------------------------------------
    // 5. 新しい現世代を評価する
    //--------------------------------------------------------
    /*
        世代交代後の ind はまだ「新しく生成された個体群」にすぎない。
        このままでは各個体の fitness が最新ではないため、
        evaluate() を呼んで再評価する必要がある。

        また evaluate() の中では sort() も行われるため、
        次世代は再び
            ind[0] が最良個体
        という秩序を持つ状態になる。
    */
    evaluate();
}


//============================================================
// 順位に基づくランキング選択で親個体を1つ選択する
// 戻り値: 選択した親個体の添え字
//============================================================
int Population::select()
{
    int num, denom, r;

    /*
        この select() は「ランキング選択」を実装している。

        ランキング選択とは、
        個体の絶対的な fitness 値そのものではなく、
        ソート後の順位に応じて選択確率を与える方法である。

        この方法の利点:
        - fitness のスケールに依存しにくい
        - 適応度差が極端でも選択圧が暴走しにくい
        - 順位だけで確率を決められる

        このコードでは sort() 後の並び順を前提としている。
        つまり、先頭に近い個体ほど高順位であり、
        より選ばれやすいように設計されている。
    */

    /*
        denom は

            POP_SIZE + (POP_SIZE - 1) + ... + 1

        の総和である。

        これは順位ごとの重みの総和であり、
        等差数列の和

            POP_SIZE * (POP_SIZE + 1) / 2

        で求めている。

        たとえば POP_SIZE = 4 のとき、

            index 0 -> 重み 4
            index 1 -> 重み 3
            index 2 -> 重み 2
            index 3 -> 重み 1

        としたいので、総和は 10 になる。
    */
    denom = POP_SIZE * (POP_SIZE + 1) / 2;

    /*
        1 以上 denom 以下の乱数 r を作る。

        ここでは rand() を何回か組み合わせて、
        より大きな範囲の乱数を作ろうとしている。

        発想としては、
        単一の rand() のビット幅だけに頼らず、
        複数回の rand() を連結して疑似的に広い整数を作る、というものである。

        その後 % denom + 1 により、
        [1, denom] の範囲へ写している。
    */
    r = ((rand() << 16) + (rand() << 1) + (rand() % 2)) % denom + 1;

    /*
        以下のループで、重み付きの区間のどこに r が落ちたかを調べる。

        num は最初 POP_SIZE から始まり、1 まで減少する。
        各 num が「ある順位の重み」を表す。

        たとえば POP_SIZE = 4 のとき、

            num = 4 なら index 0 の重み
            num = 3 なら index 1 の重み
            num = 2 なら index 2 の重み
            num = 1 なら index 3 の重み

        を意味する。

        r がその重み区間に入ったところで break し、
        対応する index を返す。
    */
    for (num = POP_SIZE; 0 < num; num--) {
        if (r <= num) {
            break;
        }
        r -= num;
    }

    /*
        戻り値は POP_SIZE - num である。

        これにより

            num = POP_SIZE     -> index 0
            num = POP_SIZE-1   -> index 1
            ...
            num = 1            -> index POP_SIZE-1

        に対応する。

        つまり、先頭の個体ほど大きい重みを持ち、
        より高い確率で親に選ばれる。

        これが Population における選択圧の具体的な実装である。

        理論的には、
        この select() によって
        「良い個体ほど子を残しやすい」
        というダーウィン的淘汰圧がアルゴリズムへ導入される。
    */
    return POP_SIZE - num;
}


/*
    ============================================================
    まとめ
    ============================================================

    この Population 実装の本質は、
    個体群を単なる保存領域としてではなく、
    進化の力学を担う構造として設計している点にある。

    - evaluate() で個体群を比較可能にする
    - sort() で順位構造を与える
    - select() で順位に基づく選択圧をかける
    - alternate() でエリート保存・交叉・突然変異・世代交代を行う

    これらの処理が組み合わさることで、
    Population 全体が「時間とともに改善される探索状態」として機能する。

    つまり Population は、
    進化計算アルゴリズムの本体そのものである。
*/
