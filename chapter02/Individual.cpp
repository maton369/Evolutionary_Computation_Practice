//==============================
// File: Individual.cpp
//==============================
#include "Individual.h"

/*
    ============================================================
    Individual.cpp
    ============================================================

    このファイルでは、Individual クラスの関数本体を置く。

    今回の要件では「適応度評価などの実装はまだ pass する」とあるため、
    ここでは空実装にしている。

    ただし、単に空にするのではなく、
    それぞれの関数が本来どこで何を担うのかをコメントで明確にしておく。

    こうしておくことで、後から実装を埋める際に
    「この関数がアルゴリズム全体のどこに位置づくのか」を見失いにくい。
*/

Individual::Individual() {
    /*
        TODO:
        初期個体の生成処理を書く場所。

        通常ここでは、
        各遺伝子座 chrom[i] をランダムに初期化する。

        初期集団の質は探索初期の広がりに影響するため、
        ランダム初期化・ヒューリスティック初期化・既知解混合など、
        問題に応じて設計の余地がある。

        現段階では設計確認のため空にしている。
    */
}

Individual::~Individual() {
    /*
        TODO:
        将来、内部で動的メモリを持たせた場合に解放処理を書く場所。

        現状は固定長配列のみなので、特別な後始末は不要である。
    */
}

void Individual::evaluate() {
    /*
        TODO:
        適応度関数を実装する場所。

        ここでは chrom の内容を問題固有の目的関数へ写像し、
        fitness に代入する。

        この関数が個体の「良し悪し」を定義するため、
        進化計算全体の目的は実質的にここで決まる。

        たとえば、
        - 最大化問題なら値が大きいほど良い
        - 最小化問題なら符号反転などで最大化形式へ揃える
        といった設計が必要になる。
    */
}

void Individual::crossover(Individual* p1, Individual* p2) {
    /*
        TODO:
        交叉処理を実装する場所。

        この関数では親 p1, p2 の染色体を参照し、
        this->chrom を子個体として構成する。

        代表的な方法:
        - 1点交叉
        - 2点交叉
        - 一様交叉
        - 順列用交叉 (PMX, OX など)

        問題の表現方法に応じて、
        「壊れてはいけない構造」が異なるため、
        交叉法は表現設計と強く結びつく。
    */
}

void Individual::mutate() {
    /*
        TODO:
        突然変異処理を実装する場所。

        代表的な方法:
        - ビット反転
        - ランダム値への置換
        - 順列要素の swap
        - ガウス雑音付加

        変異は探索の多様性維持に寄与するが、
        強すぎると既に得た良い構造を壊しやすい。

        そのため MUTATE_PROB は、
        探索と安定性のバランスを取る重要パラメータになる。
    */
}
